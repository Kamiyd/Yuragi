# Python parity reference

此目录中的 Python 实现用于生成渲染回归检查的对照数据。网页应用、开发服务器和静态构建都不依赖 Python。

```bash
python3 tools/parity/fixtures.py  # 生成对照数据
npm run parity                   # 检查 TypeScript 输出
```

当前产品说明和运行方式见仓库根目录的 [README](../README.md)。
